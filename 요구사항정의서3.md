다중 사용자 시스템 구축 시 추천 조합
Render만으로 전체 시스템을 무료 구성하기보다 아래와 같이 역할을 나누어 구성하는 것이 훨씬 안정적입니다.
•	프론트엔드 (웹 UI): Render Static Site
•	백엔드 (API): Render Web Service 1개 활용 (750시간 무료 전량 할당)
•	데이터베이스 (DB): Supabase

1. Render Static Site
Render Static Site(정적 웹사이트)는 100% 무료로 사용할 수 있습니다.
•	무료 제공량: 월 100GB의 트래픽(Bandwidth)을 무료로 제공합니다.
•	수면 모드 없음: 백엔드(Web Service)와 달리 정적 사이트는 CDN 기반으로 배포되기 때문에 15분간 접속이 없어도 서버가 잠들지 않으며(Spin-down 없음) 항상 즉시 로딩됩니다.
2. 프론트엔드와 백엔드를 모두 Render 하나로 통일
같은 대시보드(Render)에서 관리하되 기능 유형을 나누어 배치하는 것(Render Static Site + Render Web Service)이 가장 효율적이고 좋습니다.
프론트엔드 플랫폼을 Render 하나로 통일할 때의 장단점은 다음과 같습니다.
장점
•	통합 관리의 편의성: 하나의 Render 계정 및 대시보드 내에서 프론트엔드와 백엔드의 배포 로그, 환경 변수, GitHub 연동을 한눈에 관리할 수 있어 운영이 매우 단순해집니다.
•	환경 변수 연결 유연성: 동일 플랫폼 내에 있으므로 API 도메인 주소(URL) 변경 시 관리가 수월합니다.
•	비용 0원 유지: 프론트엔드는 Render Static Site (무료)로, 백엔드는 Render Web Service (무료 750시간)로 각각 등록하면 플랫폼 분리 없이도 비용이 전혀 발생하지 않습니다.
단점 및 주의점
•	백엔드 슬립 모드는 동일: 프론트엔드를 Render Static Site로 두더라도, 백엔드(Web Service)는 무료 버전 특성상 15분간 트래픽이 없으면 잠자기 상태에 빠집니다. 따라서 사용자가 오랜만에 접속하면 프론트엔드 화면은 즉시 뜨지만, 데이터를 불러오는 API 요청 시 백엔드가 깨어나는 30초~1분간의 초기 로딩이 생길 수 있습니다.
최종 권장 구성
Render 하나만 활용하셔도 무료로 훌륭하게 구동 가능합니다.
1.	프론트엔드: Render에서 Static Site로 생성하여 배포 (무료, 수면 모드 없음)
2.	백엔드: Render에서 Web Service로 생성하여 배포 (무료 750시간 지원)
3.	DB & 스토리지: Supabase 연동 (영구 무료 DB + 1GB 이미지 스토리지)
따로 플랫폼을 여러 개 쪼개지 않고 Render(프론트+백엔드) + Supabase(DB+스토리지) 2개 플랫폼만 조합하여 개발을 시작하시는 것을 강력히 추천합니다.

Render 백엔드의 슬립 모드를 방지하기 위해 외부 크론잡(Cron Job) 서비스를 활용하는 것은 실무에서 인프라 비용을 아끼기 위해 매우 자주 사용하는 효율적인 해결책입니다.
크론잡 적용의 주요 장점
•	콜드 스타트(Cold Start) 지연 해결: 10~14분 간격으로 백엔드 서버에 지속적인 HTTP 요청을 보내 서버가 잠들지 않도록 유지함으로써, 첫 접속자의 30초~1분 대기 시간을 없애줍니다.
•	비용 발생 없음: Render의 유료 플랜(월 $7~)으로 업그레이드하지 않고도 무료 플랜 환경에서 상시 가동 효과를 얻을 수 있습니다.
구현 방법 추천
1. cron-job.org 활용 (가장 간단한 노코드 방식)
•	UptimeRobot 또는 cron-job.org 같은 무료 웹 모니터링 서비스에 가입합니다.
•	백엔드 서버에 헬스체크용 엔드포인트(예: GET /health 또는 /api/ping)를 만든 후, 해당 URL을 10분~14분 주기로 주기적 호출하도록 등록합니다.
2. GitHub Actions 스케줄러 활용
•	GitHub 저장소 내에 Actions 스케줄러(cron) 작업을 작성하여 주기적으로 백엔드 URL에 curl 요청을 보내도록 설정합니다.
적용 시 유의할 점
•	월 750시간 한도 관리: Render 무료 플랜의 인스턴스 시간은 월 750시간 제공됩니다. 1개의 백엔드 서비스만 24시간 내내 켜둘 경우 월 사용량(약 744시간) 내에 수용되지만, 동일 계정에서 2개 이상의 백엔드 서비스를 동시에 24시간 켜둘 경우 월 중순에 모든 서비스가 정지됩니다.


